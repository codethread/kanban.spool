# Clojure / tools.deps
.cpcache/
.clj-kondo/
.lsp/.cache/
target/
classes/
pom.xml.asc
*.jar
.nrepl-port
.rebel_readline_history

# Editors / OS
.calva/
*.iml
.idea/
.DS_Store

# mkdocs build output; the site is published from the sources, never committed
site/

scripts/kanban-export/node_modules/
# kanban-export writes to /tmp by default; ignore any local --out overrides too
/kanban-*.html
